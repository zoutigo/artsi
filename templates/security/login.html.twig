{% extends 'base.html.twig' %}

{% block title %}Log in!
{% endblock %}

{% block body %}
	<form method="post">
		{% if error %}
			<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
		{% endif %}

		{% if app.user %}
			<div class="mb-3">
				You are logged in as
				{{ app.user.username }},
				<a href="{{ path('app_logout') }}">Logout</a>
			</div>
		{% endif %}
		{# <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required> #}


		<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
	</button>


	<section class="hero-area overlay">
		<div class="container">
			<div class="row align-items-center">
				<div class="col-lg-8 offset-lg-2 col-md-12 col-12">
					<div
						class="hero-content">
						{# <h5 class="wow fadeInUp" data-wow-delay=".2s">Expert Pleple, Quality Solutions</h5> #}
						<h1 class="wow fadeInUp" data-wow-delay=".4s">Login

						</h1>
						<div class='mb-3 mt-4'>
							<div class="text-white text-start mb-2">
								<label for="inputEmail">Email</label>
							</div>
							<input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
							<div>Le message d'aide</div>
						</div>

						<div class="mb-3">
							<div class="text-white text-start mb-2">

								<label for="inputPassword">Password</label>
							</div>
							<input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
							<div>Le message d'aide</div>
						</div>

						<label class='text-white'>
							<input type="checkbox" name="_remember_me">
							Se souvenir de moi
						</label>
						<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">


						<div
							class="button wow fadeInUp" data-wow-delay=".8s">
							{# <a href="contact.html" class="btn">Get Started</a>
																																																																																																																																																																																																																																																																																																				<a href="about-us.html" class="btn btn-alt">Know More</a> #}
							<button class="btn btn-lg btn-primary btn-alt" type="submit">
								Se Connecter
							</button>
						</div>
					</div>
				</div>
			</div>
		</div>
	</section>
</form>{% endblock %}
